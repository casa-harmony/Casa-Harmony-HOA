# ─── Casa Harmony AI — Backend dependencies ───────────────────────────────
# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Settings / validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Database (SQLAlchemy 2.0 + psycopg3 + Alembic)
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# Security
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
cryptography==44.0.0

# MFA (TOTP)
pyotp==2.9.0

# Reporting
openpyxl==3.1.5
python-docx==1.1.2
reportlab==4.2.5

# Document storage (uploaded invoices/contracts) — falls back to local disk
# when unconfigured; see app/services/documents_svc.py
cloudinary==1.41.0

# Optional in-process scheduler (nightly GL posting)
APScheduler==3.10.4

# Testing
pytest==8.3.4
httpx==0.28.1
celery[redis]==5.6.3
